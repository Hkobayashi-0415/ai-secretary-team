# Database Bootstrap (Init)

- Files under `database/init` only enable extensions and provide optional idempotent seeds.
- All tables, constraints, indexes, and data migrations are managed by Alembic under `backend/alembic`.

